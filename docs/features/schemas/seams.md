# Schemas Seams

## Known Seams

Document stable schema extension boundaries here as they are confirmed.

Initial candidates to validate before documenting deeply:

- Auth, patient, staff, and treatment Zod schemas.
- Cross-package imports from `packages/schemas/src/index.ts`.
- Runtime validation contracts shared by webapp forms and domain routes.

## Seam Entry Template

- Entry points:
- Owner:
- Invariants:
- Extension points:
- Avoid:
