# Server API Seams

## Known Seams

Document stable server API extension boundaries here as they are confirmed.

Initial candidates to validate before documenting deeply:

- Hono server entrypoint and middleware under `apps/server/src`.
- Domain server routes under `packages/domain/src/server/routes`.
- API client boundary consumed by the webapp.

## Seam Entry Template

- Entry points:
- Owner:
- Invariants:
- Extension points:
- Avoid:
