# Error Handling Seams

## Known Seams

Document stable error-handling extension boundaries here as they are confirmed.

Initial candidates to validate before documenting deeply:

- Shared base error, HTTP error, schema error, and error-code utilities.
- Domain/server translation from internal errors to HTTP responses.
- Validation error mapping across schemas, server routes, and UI forms.

## Seam Entry Template

- Entry points:
- Owner:
- Invariants:
- Extension points:
- Avoid:
