# Shared UI Seams

## Known Seams

Document stable shared UI extension boundaries here as they are confirmed.

Initial candidates to validate before documenting deeply:

- `packages/ui` component primitives, hooks, and global styles.
- Webapp shared components such as data tables, dialogs, header, badges, and filters.
- Common helpers in `apps/webapp/src/lib` and `packages/ui/src/lib`.

## Seam Entry Template

- Entry points:
- Owner:
- Invariants:
- Extension points:
- Avoid:
